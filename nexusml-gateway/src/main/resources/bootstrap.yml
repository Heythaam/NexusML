spring:
  application:
    name: gateway
  cloud:
    config:
      uri: ${CONFIG_SERVER_URI:http://localhost:8888}
      fail-fast: true
      retry:
        max-attempts: 6
        initial-interval: 1500
        max-interval: 10000
        multiplier: 1.5
